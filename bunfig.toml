# https://bun.sh/docs/runtime/bunfig
[install]
frozenLockfile = true
exact = true
saveTextLockfile = true
# Supply-chain knob: skip package versions published in the last 2 days (seconds).
minimumReleaseAge = 172800
# Our own codecs, released from cornerstonejs/codecs and pinned to exact
# versions above. We publish these ourselves, so the soak period buys nothing
# and only blocks picking up a release on the day it is cut. Unlike pnpm's
# equivalent, bun matches on package name only, so these cannot be pinned to a
# version here - the exact-version pins in the package.json files are what
# bound the exposure.
minimumReleaseAgeExcludes = [
  "@cornerstonejs/codec-big-endian",
  "@cornerstonejs/codec-charls",
  "@cornerstonejs/codec-libjpeg-turbo-8bit",
  "@cornerstonejs/codec-libjpeg-turbo-12bit",
  "@cornerstonejs/codec-little-endian",
  "@cornerstonejs/codec-openjpeg",
  "@cornerstonejs/codec-openjph",
  "@cornerstonejs/dicom-codec",
]
